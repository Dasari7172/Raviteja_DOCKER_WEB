version: "3.9"

services:
  devopsdb:
    image: dasari7172/ravi_dockerweb:dbimage
    ports:
      - "3306:3306"
    deploy:
      replicas: 3
    networks:
      - mynetwork

  application:
    image: dasari7172/ravi_dockerweb:appimage
    ports:
      - "1111:8080"
    deploy:
      replicas: 3
    depends_on:
      - devopsdb
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: overlay
